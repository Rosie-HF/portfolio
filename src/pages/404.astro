---
import Base from '@/layouts/Base.astro';

import { useTranslations, languages, defaultLocale, getLocalePath, type LanguageKey } from '@/i18n/i18n.ts';

const t = useTranslations(Astro.currentLocale as LanguageKey);

export function getStaticPaths() {
  return Object.keys(languages).map((language) => ({
    params: { language },
  }));
}
---

<Base title={t('404-1')}>
  <main class="text-center min-h-screen flex items-center justify-center flex-col">
    <h1 class="text-8xl font-black">404</h1>
    <p class="text-lg mb-8">{t('404-1')}</p>
    <a class="underline" href={getLocalePath(Astro.currentLocale as LanguageKey || defaultLocale, '/')}>{t('404-2')}</a>
  </main>
</Base>
